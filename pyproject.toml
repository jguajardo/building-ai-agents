[project]
name = "building-ai-agents"
version = "0.1.0"
description = "AI Agent learning project - From fundamentals to production"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "anthropic>=0.83.0",
]
